name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --frozen --verbose
    - name: Run tests
      run: cargo test --frozen --verbose
